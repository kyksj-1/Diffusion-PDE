%==============================================================================
% Section 5 · Experiments (~1 page) — toy validation only
%
% NeurIPS 偏好"理论重 / 实验轻"——这一节做最小可验证集合：
%   E1: Inviscid Burgers (1D)   — shock formation
%   E2: Buckley–Leverett (1D)   — non-convex flux, shock + rarefaction
%   E3: 1D Euler / Sod          — shock + contact + rarefaction
%
% Rebuttal 阶段补 E4 (shallow-water) / E5 (Vlasov–Poisson)。
%==============================================================================
\section{Experiments}
\label{sec:experiments}

% ----- Setup -----
\subsection{Setup}
\label{sec:exp:setup}

\todo{State that all experiments are toy 1D problems intended to validate the theoretical predictions of Theorems \ref{thm:improved-rate} and \ref{thm:shock-location}. Ground truth is generated with WENO5 \citep{shu1999weno}; baselines are EDM \citep{karras2022edm} and DiffusionPDE \citep{huang2024diffusionpde}; metrics are $\Wass{1}$, $L^{1}$, and shock-location error.}

% ----- E1 -----
\subsection{E1 · Inviscid Burgers}
\label{sec:exp:e1}

\todo{Show: (a) standard EDM produces oscillations near shock, (b) BV-aware parameterization \eqref{eq:bv-aware} sharpens the shock without overshoot, (c) the empirical $\Wass{1}$ scaling matches the $\varepsilon^{1/2}$ prediction of Theorem~\ref{thm:improved-rate} as $\varepsilon$ varies (training horizon ablation).}

\begin{figure}[t]
    \centering
    \fbox{\rule[-.5cm]{0cm}{4cm} \rule[-.5cm]{8cm}{0cm}}
    \caption{\todo{Figure 2: Burgers shock comparison. Left: ground-truth WENO5; middle: standard EDM (oscillation visible); right: EntroDiff (clean shock). Key take-away: BV-aware parameterization captures the shock without TV blow-up, and the residual error scales as $\varepsilon^{1/2}$ in line with Theorem~\ref{thm:improved-rate}.}}
    \label{fig:e1-burgers}
\end{figure}

% ----- E2 -----
\subsection{E2 · Buckley–Leverett}
\label{sec:exp:e2}

\todo{Non-convex flux $\flux(u) = u^{2}/(u^{2} + (1-u)^{2})$ produces a shock + rarefaction wave. EntroDiff resolves both correctly; we verify Rankine–Hugoniot speed (Theorem~\ref{thm:shock-location}) numerically.}

% ----- E3 -----
\subsection{E3 · 1D Euler (Sod shock tube)}
\label{sec:exp:e3}

\todo{Three-component system. Shock + contact discontinuity + rarefaction simultaneously. EntroDiff applied component-wise. Compare $\Wass{1}$ per component.}

\begin{table}[t]
    \caption{\todo{Table 2: main result. $\Wass{1}$ error on E1–E3, comparing EDM baseline, DiffusionPDE, FNO, and EntroDiff (ours). Key take-away: EntroDiff achieves the lowest $\Wass{1}$ on every shock-containing benchmark, with the largest gain on Sod (E3).}}
    \label{tab:main-results}
    \centering
    \begin{tabular}{lcccc}
        \toprule
        Method & E1 (Burgers) & E2 (B–L) & E3 ($\rho$) & E3 ($u$) \\
        \midrule
        FNO \citep{li2021fno}                   & --- & --- & --- & --- \\
        EDM \citep{karras2022edm}                & --- & --- & --- & --- \\
        DiffusionPDE \citep{huang2024diffusionpde} & --- & --- & --- & --- \\
        \textbf{EntroDiff (ours)}                 & --- & --- & --- & --- \\
        \bottomrule
    \end{tabular}
\end{table}

% ----- Ablation -----
\subsection{Ablation}
\label{sec:exp:ablation}

\todo{Ablate: (a) viscosity-matched schedule vs. EDM default; (b) BV-aware vs. plain parameterization; (c) each loss term in $\Lent / \Lbv / \Lburg$. Each ablation should isolate one design choice and link back to the corresponding theorem.}
